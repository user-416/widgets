{
  "name": "widgets-worker",
  "version": "0.1.0",
  "private": true,
  "description": "OAuth token-exchange Worker for Widgets (Strava). Holds the upstream client_secret values so they never live on the device.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240329.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.50.0"
  }
}
